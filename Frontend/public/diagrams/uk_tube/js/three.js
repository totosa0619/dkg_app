var camera, scene, raycaster, renderer, container;
function mainInit() {
  // Твой код для инициализации
}
function renderCircle() {
  // Твой код для рендера круга
}

function renderSector(sector) {
  // Твой код для рендера сектора (есть свойство sector.baseColor, в котором указан его цвет)
}

function renderCompany(company) {
  // Твой код для рендера компании
}

// function onCompanyChange(company) {
//   // Твой код на изменения состояния конкретной компании при фильтрации
// }