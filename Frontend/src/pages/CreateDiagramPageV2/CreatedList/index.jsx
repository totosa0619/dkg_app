import React from "react";
import WidgetsList from "../../../components/WidgetsList";

const CreatedList = ({ diagramType }) => {
  return <WidgetsList type={diagramType} />;
};
export default CreatedList;
